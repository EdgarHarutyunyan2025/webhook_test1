pipeline {
    agent any
    stages {
        stage('test for webhook') {
            steps {
                script {
                    sh "ls -la"
                }
            }
        }
    }
}

